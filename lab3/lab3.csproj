<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <Nullable>enable</Nullable>
    <UseWindowsForms>true</UseWindowsForms>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Removed MSTest package reference: test frameworks can introduce their own entry point/Main
         and should not be referenced from a WinForms application project. Add test packages
         only to dedicated test projects. -->
  </ItemGroup>

</Project>